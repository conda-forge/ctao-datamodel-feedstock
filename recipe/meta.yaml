{% set version = "1.3.1" %}
{% set python_min = "3.12" %}

package:
  name: ctao-datamodel
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/c/ctao-datamodel/ctao_datamodel-{{ version }}.tar.gz
  sha256: 25780758aa4a55b8d67a21332366c70c4615d7875f6b9de7f11923de3643ca92

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=65
    - setuptools-scm >=8
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - pydantic
    - email-validator
    - pyyaml
    - astropy-base >=7.1
    - astropydantic
    - particle

test:
  imports:
    - ctao_datamodel
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/ctao-datamodel/
  summary: CTAO Data Model in pydantic and utilities for exporting it to latex and plantuml.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - kosack
    - maxnoe
