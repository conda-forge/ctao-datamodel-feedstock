{% set version = "1.2.1" %}
{% set python_min = "3.12" %}

package:
  name: ctao-datamodel
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/c/ctao-datamodel/ctao_datamodel-{{ version }}.tar.gz
  sha256: 7a0b92a17bec0f9837d3eedbd5b486ea8a26bac081c8f599373765096066050a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=65
    - setuptools-scm >=8
    - pip
  run:
    - python >={{ python_min }}
    - numpy
    - pydantic
    - email-validator
    - pyyaml
    - astropy-base >=7.1
    - astropydantic

test:
  imports:
    - ctao_datamodel
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://pypi.org/project/ctao-datamodel/
  summary: CTAO Data Model in pydantic and utilities for exporting it to latex and plantuml.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - kosack
    - maxnoe
